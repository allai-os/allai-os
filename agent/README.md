# Agent core (Python)

Cerebro de allAI OS: capa de proveedores, router híbrido, registro de herramientas, memoria, voz.

**Estado**: estructura pendiente. Ver [ROADMAP.md](../ROADMAP.md) fase L — Link.

## Subcarpetas previstas

- `core/` — provider abstraction, router, sesiones
- `providers/` — claude.py, ollama.py, futuros
- `tools/` — mouse, keyboard, shell, fs, browser, voz
- `sandbox/` — bubblewrap wrapper
- `permissions/` — capability system
- `memory/` — memoria persistente cifrada
- `tests/`
