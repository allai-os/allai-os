# System components (Rust)

Componentes de sistema: daemon, CLI, kill-switch, audit log.

**Estado**: estructura pendiente. Ver [ROADMAP.md](../ROADMAP.md) fase L — Launch.

## Subcarpetas previstas

- `allaid/` — daemon systemd-user, expone D-Bus `org.allai.Agent1`
- `allai-ctl/` — CLI de control
